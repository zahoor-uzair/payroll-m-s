import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#334d50",
    },
    secondary: {
      main: "#cbcaa5",
    },
  },
});
